name: CI
on:
    pull_request:
    push:
        branches: [main]
jobs:
    quality:
        name: Kvalitetsjobbet
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@4
            - uses: actions/setup-node@4
              with: { node-version: 22, cache: npm }
            - run: npm ci